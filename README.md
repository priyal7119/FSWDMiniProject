
  # Convert TSX to JSX

  This is a code bundle for Convert TSX to JSX. The original project is available at https://www.figma.com/design/6kaoQCHhhf6fUHKEbF29EH/Convert-TSX-to-JSX.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  